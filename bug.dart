```dart
List<int> numbers = [1, 2, 3, 4, 5];

void main() {
  // This will throw an error because it tries to access an index that is out of bounds.
  print(numbers[5]); // IndexOutOfBoundsException
}
```